---
title: 新建编辑模块知识库
tags: [产品, 低代码, 字段, 计算, 布局]
---

# 新建编辑模块知识库

## 一、计算类

### 1.1 公式字段

前端只计算，不提交，真正的计算逻辑都放在后台。因为有些公式会用到下游对象的一些信息，这些信息在新建之后是可以接着改的。主要的计算逻辑都在 `paas-expression-calculation` 组件库里面。

---

### 1.2 累计汇总

#### 1.2.1 top累计汇总

累计下游对象的某个字段，根据另一个排序字段的大小取第一条数据的值，空默认是最小。

#### 1.2.2 累计汇总

累计下游对象的某个数值类的字段，还可以累计自己的行数之类的东西。

---

### 1.3 默认值

当字段依赖的计算字段发生变化时，字段的结果会自动发生变化，还可以给字段赋予一个初始化的值。

**示例：**
比如 A 字段的值是 5，B 字段值是 10，给 C 字段设置 `A + B`。当第一次打开新建编辑面的时候值（初始值）为 10，A 字段从 5 变成 10 的时候 C 的值会变成 15。

> 对于默认值来说，如果出现了不稳定的环，代码就会陷入死循环。

---

### 1.4 字段依赖

可以通过一个选项值集类型、单选、多选字段控制另一个选项值集、单选字段的结果，限制其选定范围。当控制字段发生变化的时候，结果字段需要被清空。

**示例：**
比如当 A 字段为"夏天"的时候，B 字段只能选择荷花；A 字段是春天的时候，B 字段只能选择牡丹。当 A 字段切换的时候，B 字段的选择范围变更并且 B 字段需要清空。

---

### 1.5 预置筛选器

预置筛选器是 SaaS 内置进去的预置筛选条件。当他的筛选器含有字段类型的筛选器时，如果查询字段手动发生了变化，当前查找关系字段（包括动态查找关系字段）清空。

**示例：**
当查找关系字段 A 配置了"实数等于B字段"，B 字段是一个实数型字段：
- 如果是通过默认值从 3 变成了 5，则 A 字段**不清空**。
- 如果是手动从 3 变成了 5，则 A 字段**清空**。

---

### 1.6 父汇子

#### 1.6.1 树形标准对象（父汇子）

父汇子的字段由服务端计算（因为此时候的树形数据只可以一条一条建）。

#### 1.6.2 树形子对象

由前端自己计算，从最末级往上面一层一层往上汇。

**说明：**
树形特有的字段类型，父节点的值由子节点累积而来，当前父节点累积自己的下一级，从最后一层往上汇。如果是在中间的层级改了某个字段，可能会出现：改的这行往上的父汇子是对的，下面的累到这行是不对的。

**示例：**

有一棵树，层级为 `1.1.1.2  1.1.1`，实数 A 的值为空，当给 `1.1.1` 的实数 A 改成 7 之后：

```
1     A 7
1.1   A 7
1.2   A 空
1.1.1 A 7
```

给 `1.2` 的 A 改成 8，得到：

```
1     A 15
1.1   A 7
1.2   A 8
1.1.1 A 7
```

给 `1.1.1` 的 A 改成 20，得到：

```
1     A 28
1.1   A 20
1.2   A 8
1.1.1 A 7
```

---

## 二、样式 / 布局类

### 2.1 主对象字段排列方式

- **一种**是配置端配置的基础信息排列方式，决定配置在页面布局的详细资料里，里面有一个默认布局。
- **一种**是可以在前台自己设置。

---

### 2.2 字段显示条件

- 主对象字段不符合显示条件，页面上**隐藏**。
- 子对象的字段不符合显示条件，页面上列表里面显示**禁用**，并且看不到之前的值，不隐藏。

> **说明：**
> 通过选项控制和高级条件来控制字段的显示隐藏。如果勾选了"不符合满足条件为空值"，在提交的时候会对不符合条件的值进行清空；如果没勾选，提交时也会对该字段进行提交，只是当条件不满足时看不见。
> 显示条件不实际控制用户的数据，只控制字段显隐。"不符合满足条件为空"的开关也仅在提交时生效。

---

### 2.3 字段配置

#### 2.3.1 字段必填条件

通过选项控制和高级条件来控制字段的必填。字段必填优先于条件必填，低代码控制的必填也优先于条件必填。必填的逻辑对于 `*` 号部分放在页面渲染阶段，还有就是校验阶段。

#### 2.3.2 字段只读条件

通过选项控制和高级条件来控制字段的只读，目前处理时机放在了页面渲染阶段。

---

## 三、布局类

### 3.1 关联创建配置

#### 3.1.1 排列方式

关联创建对象的排列方式：**向下平铺**、**多标签页**，两种展示方式。

#### 3.1.2 批量按钮设置

| 配置项 | 说明 |
|---|---|
| 按钮列表 | 取决于该关联创建对象本身有哪些查找关系字段 |
| 隐藏 | 控制批量按钮的展示隐藏 |
| 去重 | 只有非树形按钮才有；配置后通过批量按钮添加的数据，当前按钮关联的查找关系不能有重复；开启去重后，单条添加按钮禁用，只能通过批量按钮添加 |

#### 3.1.3 按主对象数据批量选择

只有主子类型的查找关系按钮才会有，勾选之后自动出现"选主自动添加子"按钮。

- **开启**：选主的时候对子做筛选，并选中筛选出来所有的子。
- **关闭**：选主的时候只对子做筛选，不选中。

#### 3.1.4 快速录入

批量选择查找关系字段页面时，可以录入主表的 1 个字段，并在主表的字段上反显录入的数值，实际上跟手动去批量编辑这个字段一样。

#### 3.1.5 汇总字段设置

- **普通计算**：计算某个字段的数值总和，可以设置单位（例如千、万、百万、亿）。
- **仅计算第一级**：对于树形子对象来说，就是计算第一层节点的值。

#### 3.1.6 关联创建对象基础配置

| 配置项 | 说明 |
|---|---|
| 行复制 | 控制关联创建对象行上的复制按钮，开启就显示，不开启就隐藏 |
| 导入 | 控制前台关联创建对象上的导入按钮 |
| excel录入 | 控制关联创建对象顶部的 excel 模式 |
| 显示/隐藏 | 控制关联创建对象在新建编辑页的展示隐藏 |

**必填设置：**
- 勾选了必填，没配置筛选条件 → 一定必填。
- 勾选了必填，配置了筛选条件 → 当符合条件的时候才必填生效。

---

### 3.2 布局属性

- **详情页低代码插件**：通过低代码可以在详情页执行一些特殊的逻辑。
- **新建编辑低代码插件**：通过低代码可以在新建编辑页定制一些特殊的逻辑。

---

### 3.3 批量编辑按钮

- **顶部批量按钮**：在表格顶部的批量按钮。
- **表头批量按钮**：在有选中行的基础下，鼠标 hover 到表头上部分时，弹出一个 popover 帮助用户快速填充字段，只对标准子生效，并且只对可以编辑的字段生效。
- **字段右侧批量修改**：
  - 普通子：竖列填充、向上填充。
  - 树形子：添加同级、添加子级。
  - 字段右侧有一个小图标，可以通过这个按钮快速把当行的数据添加给别的行。

> **说明：** 本质实现都采用的是批量编辑的计算方法。

---

### 3.4 excel 粘贴

| 类型 | 说明 |
|---|---|
| 查找关系字段 | 需要通过服务端的接口把用户粘贴的文本转化成完整的查找关系 |
| 普通字段 | 通过前端自己的格式化方法，对解析出来的文本做转化，比如单选、多选、选项值集、省市区等根据规则解析 |
| 多字段匹配 | 通过粘贴查找关系下的两个字段匹配出一个查找关系，比如查找关系是 A，有 B 字段和 C 字段，当 B 和 C 能确定唯一查找关系时粘贴两列就可以带出查找关系的值 |
| 树形子 | 只能追加，不能把数据同步到 excel 模式 |
| 标准子 | 可以同步数据到 excel 模式 |

---

### 3.5 excel 模式

**计算规则：** excel 模式的字段发生变化时，只计算默认值和公式（SaaS 包括特殊的字段筛选之类的都不执行），也不往上累计汇总。

**excel 是否开启虚拟表格：**
- **开（走 newExcelTable）**：因为会出现用户粘贴的数据量特别大，如果不走虚拟表格会出现上万个单元格；但开启虚拟表格之后高度会固定，通过一个租户开关去决定是否开启。
- **关（走 excelTable）**

> 项目里目前采用的是 `react-datasheet` 插件实现的，在此基础上魔改了一个新的带虚拟的 `dataSheet` 组件。excel 模式为了不影响老代码，目前代码有两份。

> **状态同步注意点：** excel 模式状态同步很重要。比如在 excel 里面删除了某个之前已经存在的数据，就得打上删除的标记，数据里面的标记必须和页面保持一致；修改的标记，该过的行一定要打上修改标记。excel 模式确定时需要对当前子的累积汇总重新进行计算，如果累积的值发生了变化也会对父对象的其他计算逻辑产生影响。

---

## 四、新建编辑模块

### 4.1 假导入

- **覆盖导入**：导入会覆盖已有数据。
- **子对象：**
  - 追加导入：导入会在原有数据的基础上追加数据。
  - 多字段匹配：通过导入查找。

> **说明：** 导入的字段自身不计算默认值，但是被他们影响的字段计算都进行。树形子因为有父汇子，所以任何计算都是从最下面一行开始计算，因为涉及到父汇子，目前这里的 SaaS 逻辑还有些欠缺，所以如果导入之后 SaaS 逻辑不对让产品演进。

---

### 4.2 批量新增按钮

- **查找关系批量按钮**：本质实现都是基于 `BaseSelectTable` 实现，动态查找关系唯一特点是多一个 `metaLabel`。
- **动态查找关系批量按钮**
- **批量添加员工按钮**
- **SaaS 批量按钮**

---

### 4.3 AI 录入

通过 AI 的图片识别来识别主对象和子对象的信息，修改主对象信息和覆盖子对象的信息。识别过来的值在这次计算过程中不能被修改。

---

### 4.4 自动填充

**配置：** 在配置端标准对象里面安装自动填充应用，然后选择查找关系的自动填充，就可以配置一个自动查找关系填充按钮。

**功能：** 主要针对用户需要手动批量填写一些符合某些数据条件的查找关系。

> **注意：** 假设之前的值是空，填充之后的值还是空，这次填充是不算的，需要跳出去。只有"之前无值到有值"或者"有值不管是变自己还是变其他"都算是有效的填充。

---

### 4.5 汇总子表

**配置：** 在配置端里面安装汇总子表应用，然后新增汇总子表设置。

**功能：** 汇总子表主要是把除数值外相同的数据汇聚到一条，可以通过回填明细去批量修改那一批相同数据的数值类的值。

---

### 4.6 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+S` | 保存 |
| `Alt+1` | 当前行下新建 / 添加同级 |
| `Tab` | 跳转到下一个输入框 |
| `Shift+Tab` | 跳到上一个输入框 |
| 上下左右 | 跳转单元格 |
| `Alt+2` | 子表文本框放大 / 恢复 |
| `Enter` | 下拉框调出下拉菜单 |

> **注意：** 因为存在假框，除了 `Ctrl+S` 之外的快捷键只有激活的当前行才会生效；当用上一行下一行跳转单个格时，没有加载的假框会先转化成真行才会激发聚焦。
>
> `Ctrl` 也有注意的点：在进行聚焦的时候，键盘上的 Ctrl 区域一般发生聚焦之后，所有打开的新建编辑都有可能被触发——之前加了一个多页签的控制，每个页签打开的新建编辑会进一个数组，先判断是哪个页签，然后后进的就是最新打开的。这样就可以防止多页签之前的快捷键串了，或者二级的跟一级的串了。

---

### 4.7 全屏编辑

客户可能会有多个关联创建对象，特别多的时候添加东西可视区域太小，所以需要通过全屏编辑去编辑当前想看到的这个子。

---

### 4.8 假框

假框主要是为了解决当行和列特别多的时候，页面渲染的 `input` 标签节点特别多，为了优化页面性能增加的一个展示模式。

- **非全屏模式**：假框显示的是假的、长得像 `input` 的 `div`。
- **全屏模式**：假框就是一个单层的 `div`。

这样设计是因为设计想在假框下看着跟普通的输入框无区别，但是全屏下的内容实在太多了，复杂的 VDom 节点也会特别多，所以在全屏模式下略有不同。另外，上传组件和几个特殊的组件有一些逻辑不能用假框去做，根据类型做了区分。

---

## 五、场景类

### 5.1 新建

#### 5.1.1 计算范围

新建的时候不管字段是否在布局上都会计算，所以在生成计算器的时候不需要传入 `fields`（不传默认就是全部生成）。

#### 5.1.2 默认值

| 配置 | 说明 |
|---|---|
| 默认值重计算**开** | 如果依赖字段发生了变化，不管这个字段有没有被手动修改过，默认值都可以重新计算 |
| 默认值重计算**关** | 如果依赖字段发生了变化，并且这个字段没有被手动修改过，那么默认值就可以重新计算 |
| 公式和引用字段 | 只要依赖项发生了变化，一定会计算 |

---

### 5.2 编辑

#### 5.2.1 计算范围

| 配置 | 说明 |
|---|---|
| 未开启"支持重新计算页面上不显示的字段" | 只计算布局上展示的字段 |
| 开启"支持重新计算页面上不显示的字段" | 不管在不在布局上都会计算 |

#### 5.2.2 默认值

| 配置 | 说明 |
|---|---|
| 默认值重计算**开** | 如果依赖字段发生了变化，不管这个字段有没有被手动修改过，默认值都可以重新计算 |
| 默认值重计算**关** | 不管依赖项变没变，始终不计算默认值 |
| 公式和引用字段 | 只要依赖项发生了变化，一定会计算 |

---

### 5.3 复制

| 类型 | 行为 |
|---|---|
| 标准子 | 一条一条子复制，把原有字段带过来之后进行一次默认值计算，原有的值这次计算不发生改变 |
| 树形子 | 一条一条子复制，把原有字段带过来之后进行一次默认值计算；所有的子复制完之后，进行一次父汇子计算从末级开始 |
| 主对象 | 复制所有有值的字段，进行一次默认值计算，原有的值这次不发生计算 |

> **说明：** 对于所有的子复制完之后需要去进行累积汇总计算，保证计算结果正确。复制时如果有图片和附件类型的字段，需要去调用服务端的接口生成新的地址和实例，要不然删除图片的时候实际上没有真正删除。

---

### 5.4 对象转化

#### 5.4.1 转化

- **批量转化**：通过服务端接口去做数据转化。在查询查找关系的时候需要把用来计算的下游字段都传过去，然后拿到服务端数据之后进行一个数据的初始化计算。
- **单个转化按钮**：
  - **主转主**：前端自己去转化数据，查找关系字段和动态查找关系字段通过服务端的接口去转化，剩余的字段通过方法去解析。
  - **主转子**
  - **子转子**

#### 5.4.2 拉取

配置了 A→B 的对象转化，那么在 B 对象上新建的时候去选择 A 查找关系字段，就可以把 A→B 的转化信息都带过来。

---

### 5.5 变更

变更是新建的一种，但是不变更生效的内容不会影响原单据，可以想象成是新建的另一种复制，其计算逻辑按照新建的逻辑走。

- **编辑变更**
- **布局：**
  - 变更单布局：变更单有自己的布局。
  - 原对象布局：变更单使用原对象的布局，一般原对象和变更单对象的业务类型都会一致。
- **多次变更**：需要后台通过代码开启。

---

### 5.6 其他场景

- 自定义按钮（需配置）
- 审批流
- 添加成员
- 业务流
- 工作流
- 草稿箱
